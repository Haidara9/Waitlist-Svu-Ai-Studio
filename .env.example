# Firebase Web API key (public client key). Required to initialize Firebase.
# Get it from Firebase Console → Project settings → Your apps → Web app → apiKey.
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-web-api-key
